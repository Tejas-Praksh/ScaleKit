apiVersion: v1
kind: ServiceAccount
metadata:
  name: scalekit-sa
  namespace: scalekit
  labels:
    app: scalekit
# Minimal permissions principle: disable auto-mounting of service account tokens.
# The application does not need to call the Kubernetes API.
automountServiceAccountToken: false
