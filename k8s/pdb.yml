apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: scalekit-pdb
  namespace: scalekit
spec:
  minAvailable: 1  # Always keep at least 1 pod running, even during node maintenance
  selector:
    matchLabels:
      app: scalekit
